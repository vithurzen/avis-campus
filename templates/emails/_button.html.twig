{% macro cta(url, label) %}
    <p style="margin: 24px 0;">
        <a href="{{ url }}" style="display: inline-block; background: #2563eb; color: #ffffff; text-decoration: none; padding: 10px 20px; border-radius: 6px; font-size: 14px;">
            {{ label }}
        </a>
    </p>
{% endmacro %}
